research_notes/
.idea/
.DS_Store

# Build artifacts
/target

# Runtime data — generated when the server runs. Default location is
# now ~/.facetql (see config.rs), not the repo root, but these are
# still ignored in case ENOCHIAN_DATA_DIR or --data-dir points at "."
facetql.data
facetql.wal
facetql.edges
facetql.tombstones

# rustfmt backup files
**/*.rs.bk

# ----------------
# Go helper tool (fa/facetql.go, no go.mod of its own) — if ever built
# directly with `go build`, it leaves a binary named after the dir/package.
# ----------------
fa/facetql
fa/fa
